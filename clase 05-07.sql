/* CENTRO CULTURAL */

CREATE TABLE epocas(
    nombre VARCHAR(50) NOT NULL,
    caracteristicas VARCHAR(50) NOT NULL,
    domicilio VARCHAR(70) NOT NULL,
    telefono INT(10),
    PRIMARY KEY(nombre)
)
